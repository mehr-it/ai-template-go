=== Task 10: docker_inner/*.sh placeholder rewrite + service-injection markers ===
Date: 2026-07-31T10:27:36+02:00

======================================================================
DIFF: docker_inner/up.sh
======================================================================
--- /tmp/task-10-backup/up.sh	2026-07-31 10:21:40.027572734 +0200
+++ docker_inner/up.sh	2026-07-31 10:27:07.149399429 +0200
@@ -6,16 +6,16 @@
 # shellcheck source=lib/slug.sh
 source "${SCRIPT_DIR}/lib/slug.sh"
 
-wasi_resolve_worktree_root
-wasi_assert_not_main_checkout
-wasi_derive_slug >/dev/null
+__project_prefix___resolve_worktree_root
+__project_prefix___assert_not_main_checkout
+__project_prefix___derive_slug >/dev/null
 
-cd "${WASI_WORKTREE_ROOT}"
+cd "${__PROJECT_PREFIX___WORKTREE_ROOT}"
 
-echo "[docker_inner] slug=${WASI_STACK_SLUG} worktree=${WASI_WORKTREE_ROOT}"
+echo "[docker_inner] slug=${__PROJECT_PREFIX___STACK_SLUG} worktree=${__PROJECT_PREFIX___WORKTREE_ROOT}"
 
 # Acquire flock to serialize port-discovery against concurrent up.sh invocations
-LOCK_DIR="${WASI_WORKTREE_ROOT}/docker_inner/lock"
+LOCK_DIR="${__PROJECT_PREFIX___WORKTREE_ROOT}/docker_inner/lock"
 mkdir -p "${LOCK_DIR}"
 LOCK_FILE="${LOCK_DIR}/up.lock"
 
@@ -23,10 +23,10 @@
   flock -x 200
 
   # Bring stack up (--wait blocks until all healthchecks green).
-  # signal-cli is a JVM: cold start runs ~15-20s, so allow generous headroom.
+  # Timeout accommodates slow-starting services (JVM cold starts, DB init, etc.).
   docker compose \
-    -p "wasi-${WASI_STACK_SLUG}" \
-    -f "${WASI_WORKTREE_ROOT}/docker_inner/docker-compose.yml" \
+    -p "wasi-${__PROJECT_PREFIX___STACK_SLUG}" \
+    -f "${__PROJECT_PREFIX___WORKTREE_ROOT}/docker_inner/docker-compose.yml" \
     up -d --wait --wait-timeout 180
 
   # Discover dynamic ports (poll until non-empty, max 20 attempts × 5s)
@@ -34,8 +34,8 @@
     local svc="$1" port="$2" result
     for _ in $(seq 1 20); do
       result="$(docker compose \
-        -p "wasi-${WASI_STACK_SLUG}" \
-        -f "${WASI_WORKTREE_ROOT}/docker_inner/docker-compose.yml" \
+        -p "wasi-${__PROJECT_PREFIX___STACK_SLUG}" \
+        -f "${__PROJECT_PREFIX___WORKTREE_ROOT}/docker_inner/docker-compose.yml" \
         port "${svc}" "${port}" 2>/dev/null | cut -d: -f2)"
       if [[ -n "${result}" ]]; then
         printf '%s' "${result}"
@@ -47,41 +47,30 @@
     return 1
   }
 
-  SIGNAL_HTTP_PORT="$(_get_port signal-cli 8080)"
-  POSTGRES_PORT="$(_get_port postgres 5432)"
+  # BOOTSTRAP_SERVICE_PORTS — port-discovery + sed stanzas appended by bootstrap when services are selected
 
-  # Check idempotency: if .env.testing already has these exact ports, skip rewrite
-  ENV_FILE="${WASI_WORKTREE_ROOT}/.env.testing"
-  TEMPLATE="${WASI_WORKTREE_ROOT}/docker_inner/.env.testing.template"
-  TMPFILE="${WASI_WORKTREE_ROOT}/.env.testing.tmp"
-
-  if [[ -f "${ENV_FILE}" ]] && \
-     grep -q "127.0.0.1:${SIGNAL_HTTP_PORT}" "${ENV_FILE}" && \
-     grep -q "127.0.0.1:${POSTGRES_PORT}" "${ENV_FILE}"; then
-    echo "[docker_inner] stack already up with matching ports — skipping env file rewrite."
-  else
-    # Generate env file from template (atomic: write to .tmp, then mv)
-    sed \
-      -e "s/__SIGNAL_HTTP_PORT__/${SIGNAL_HTTP_PORT}/g" \
-      -e "s/__POSTGRES_PORT__/${POSTGRES_PORT}/g" \
-      "${TEMPLATE}" > "${TMPFILE}"
-
-    # Validate: no placeholders remain
-    if grep -q '__' "${TMPFILE}"; then
-      echo "up.sh: generated env file still contains placeholders:" >&2
-      grep '__' "${TMPFILE}" >&2
-      rm -f "${TMPFILE}"
-      exit 1
-    fi
-
-    # Atomic publish
-    mv "${TMPFILE}" "${ENV_FILE}"
-    echo "[docker_inner] wrote ${ENV_FILE}"
+  ENV_FILE="${__PROJECT_PREFIX___WORKTREE_ROOT}/.env.testing"
+  TEMPLATE="${__PROJECT_PREFIX___WORKTREE_ROOT}/docker_inner/.env.testing.template"
+  TMPFILE="${__PROJECT_PREFIX___WORKTREE_ROOT}/.env.testing.tmp"
+
+  # Generate env file from template (atomic: write to .tmp, then mv).
+  # Bootstrap injects sed -i substitutions on ${TMPFILE} at the marker above.
+  cp "${TEMPLATE}" "${TMPFILE}"
+
+  # Validate: no placeholders remain
+  if grep -q '__' "${TMPFILE}"; then
+    echo "up.sh: generated env file still contains placeholders:" >&2
+    grep '__' "${TMPFILE}" >&2
+    rm -f "${TMPFILE}"
+    exit 1
   fi
 
-  echo "[docker_inner] stack 'wasi-${WASI_STACK_SLUG}' is up."
-  echo "[docker_inner] signal-cli  127.0.0.1:${SIGNAL_HTTP_PORT}  (JSON-RPC /api/v1/rpc, SSE /api/v1/events)"
-  echo "[docker_inner] postgres    127.0.0.1:${POSTGRES_PORT}"
+  # Atomic publish
+  mv "${TMPFILE}" "${ENV_FILE}"
+  echo "[docker_inner] wrote ${ENV_FILE}"
+
+  echo "[docker_inner] stack 'wasi-${__PROJECT_PREFIX___STACK_SLUG}' is up."
+  # BOOTSTRAP_SUMMARY_LINES — service summary lines appended by bootstrap
   echo "[docker_inner] env file: ${ENV_FILE}  (NEVER COMMIT — generated artifact)"
   echo "[docker_inner] run tests with: TEST_ENV_FILE=${ENV_FILE} go test ..."
 

======================================================================
DIFF: docker_inner/down.sh
======================================================================
--- /tmp/task-10-backup/down.sh	2026-07-31 10:21:40.027656119 +0200
+++ docker_inner/down.sh	2026-07-31 10:27:05.613409483 +0200
@@ -6,26 +6,26 @@
 # shellcheck source=lib/slug.sh
 source "${SCRIPT_DIR}/lib/slug.sh"
 
-wasi_resolve_worktree_root
-wasi_assert_not_main_checkout
-wasi_derive_slug >/dev/null
+__project_prefix___resolve_worktree_root
+__project_prefix___assert_not_main_checkout
+__project_prefix___derive_slug >/dev/null
 
-cd "${WASI_WORKTREE_ROOT}"
+cd "${__PROJECT_PREFIX___WORKTREE_ROOT}"
 
-echo "[docker_inner] bringing down stack 'wasi-${WASI_STACK_SLUG}'..."
+echo "[docker_inner] bringing down stack 'wasi-${__PROJECT_PREFIX___STACK_SLUG}'..."
 
-# -v wipes volumes, which includes the signal-cli linked-device credentials.
+# -v wipes volumes (any service credentials or persisted state are lost).
 docker compose \
-  -p "wasi-${WASI_STACK_SLUG}" \
+  -p "wasi-${__PROJECT_PREFIX___STACK_SLUG}" \
   -f docker_inner/docker-compose.yml \
   down -v --remove-orphans
 
 # Remove generated .env.testing if it was written by up.sh
 # Heuristic: check for the "Generated by docker_inner/up.sh" header comment
-ENV_FILE="${WASI_WORKTREE_ROOT}/.env.testing"
+ENV_FILE="${__PROJECT_PREFIX___WORKTREE_ROOT}/.env.testing"
 if [[ -f "${ENV_FILE}" ]] && grep -q '# Generated by docker_inner/up.sh' "${ENV_FILE}"; then
     rm -f "${ENV_FILE}"
     echo "[docker_inner] removed generated .env.testing"
 fi
 
-echo "[docker_inner] stack 'wasi-${WASI_STACK_SLUG}' is down."
+echo "[docker_inner] stack 'wasi-${__PROJECT_PREFIX___STACK_SLUG}' is down."

======================================================================
DIFF: docker_inner/reset.sh
======================================================================

======================================================================
DIFF: docker_inner/status.sh
======================================================================
--- /tmp/task-10-backup/status.sh	2026-07-31 10:21:40.027812958 +0200
+++ docker_inner/status.sh	2026-07-31 10:25:55.080853925 +0200
@@ -6,33 +6,29 @@
 # shellcheck source=lib/slug.sh
 source "${SCRIPT_DIR}/lib/slug.sh"
 
-wasi_resolve_worktree_root
-wasi_assert_not_main_checkout
-wasi_derive_slug >/dev/null
+__project_prefix___resolve_worktree_root
+__project_prefix___assert_not_main_checkout
+__project_prefix___derive_slug >/dev/null
 
-cd "${WASI_WORKTREE_ROOT}"
+cd "${__PROJECT_PREFIX___WORKTREE_ROOT}"
 
-echo "[docker_inner] stack: wasi-${WASI_STACK_SLUG}"
+echo "[docker_inner] stack: wasi-${__PROJECT_PREFIX___STACK_SLUG}"
 echo ""
 
 # Show raw compose ps output
 docker compose \
-  -p "wasi-${WASI_STACK_SLUG}" \
+  -p "wasi-${__PROJECT_PREFIX___STACK_SLUG}" \
   -f docker_inner/docker-compose.yml \
   ps 2>/dev/null || true
 
 echo ""
 
 # Build port table
-SIGNAL_HTTP_PORT="$(docker compose -p "wasi-${WASI_STACK_SLUG}" -f docker_inner/docker-compose.yml port signal-cli 8080 2>/dev/null | cut -d: -f2 || echo '-')"
-POSTGRES_PORT="$(docker compose -p "wasi-${WASI_STACK_SLUG}" -f docker_inner/docker-compose.yml port postgres 5432 2>/dev/null | cut -d: -f2 || echo '-')"
-
 printf "%-20s %s\n" "SERVICE" "ADDR"
-printf "%-20s %s\n" "signal-cli" "127.0.0.1:${SIGNAL_HTTP_PORT}"
-printf "%-20s %s\n" "postgres"   "127.0.0.1:${POSTGRES_PORT}"
+# BOOTSTRAP_STATUS_ROWS — service rows appended by bootstrap
 
 # Exit code: 0 if at least one container running, 1 otherwise
-RUNNING="$(docker compose -p "wasi-${WASI_STACK_SLUG}" -f docker_inner/docker-compose.yml ps --status running --quiet 2>/dev/null | wc -l)"
+RUNNING="$(docker compose -p "wasi-${__PROJECT_PREFIX___STACK_SLUG}" -f docker_inner/docker-compose.yml ps --status running --quiet 2>/dev/null | wc -l)"
 if [[ "${RUNNING}" -gt 0 ]]; then
     exit 0
 else

======================================================================
DIFF: docker_inner/prune.sh
======================================================================
--- /tmp/task-10-backup/prune.sh	2026-07-31 10:21:40.027877582 +0200
+++ docker_inner/prune.sh	2026-07-31 10:21:48.244043835 +0200
@@ -4,7 +4,7 @@
 # Prune orphan wasi-* compose stacks whose worktree no longer exists.
 # Standalone — does NOT source lib/slug.sh (operates across all stacks).
 
-MAIN_CHECKOUT="${WASI_MAIN_CHECKOUT:-/home/ubuntu/workspace}"
+MAIN_CHECKOUT="${__PROJECT_PREFIX___MAIN_CHECKOUT:-/home/ubuntu/workspace}"
 
 YES=false
 for arg in "$@"; do
@@ -19,7 +19,7 @@
 
 # Collect wasi-* compose projects
 mapfile -t PROJECTS < <(docker compose ls --format json 2>/dev/null | \
-    jq -r '.[] | select(.Name | startswith("wasi-")) | .Name' 2>/dev/null || true)
+    jq -r '.[] | select(.Name | startswith("__project_prefix__-")) | .Name' 2>/dev/null || true)
 
 if [[ ${#PROJECTS[@]} -eq 0 ]]; then
     echo "[docker_inner] no orphan wasi-* stacks found."

======================================================================
VERIFICATION CHECKS
======================================================================

--- Forbidden strings sweep (must be empty): ---
(none — PASS)

--- prune.sh hyphen check (must be empty): ---
(none — PASS)

--- prune.sh __project_prefix__- (must exist): ---
22:    jq -r '.[] | select(.Name | startswith("__project_prefix__-")) | .Name' 2>/dev/null || true)

--- Marker counts (each must be 1): ---
BOOTSTRAP_SERVICE_PORTS in up.sh:    1
BOOTSTRAP_SUMMARY_LINES in up.sh:    1
BOOTSTRAP_STATUS_ROWS  in status.sh: 1

--- bash -n on all 5 scripts: ---
OK: docker_inner/down.sh
OK: docker_inner/prune.sh
OK: docker_inner/reset.sh
OK: docker_inner/status.sh
OK: docker_inner/up.sh

--- _get_port helper preserved in up.sh: ---
33:  _get_port() {

--- flock preserved in up.sh: ---
17:# Acquire flock to serialize port-discovery against concurrent up.sh invocations
23:  flock -x 200

--- atomic tmpfile→mv pattern preserved in up.sh: ---
54:  TMPFILE="${__PROJECT_PREFIX___WORKTREE_ROOT}/.env.testing.tmp"
57:  # Bootstrap injects sed -i substitutions on ${TMPFILE} at the marker above.
58:  cp "${TEMPLATE}" "${TMPFILE}"
61:  if grep -q '__' "${TMPFILE}"; then
63:    grep '__' "${TMPFILE}" >&2
64:    rm -f "${TMPFILE}"
69:  mv "${TMPFILE}" "${ENV_FILE}"

--- down -v preserved in down.sh: ---
21:  down -v --remove-orphans
