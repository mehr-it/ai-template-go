Task 13: Overwrite docker_inner/.env.testing.template with generic header + BOOTSTRAP_ENV_LINES marker

ACCEPTANCE CRITERIA:
✓ Contains '# BOOTSTRAP_ENV_LINES' marker
✓ Contains '# Generated by docker_inner/up.sh' header (down.sh deletion heuristic)
✓ Does NOT contain 'WASI_SIGNAL', 'whatsmeow', or hardcoded ports
✓ Verification passed: grep -q '# BOOTSTRAP_ENV_LINES' && ! grep -qE 'WASI_SIGNAL|whatsmeow' && grep -q '# Generated by docker_inner/up.sh'

FILE CONTENT:
---
# Generated by docker_inner/up.sh — DO NOT COMMIT.
# Ports are dynamic and change every time the stack restarts.
#
# Placeholder pattern: use __<SERVICE>_PORT__ as a sed target; up.sh replaces
# each with the actual port discovered from `docker compose port <svc> <port>`.
#
# Example (uncomment and adapt after adding a service):
# __PROJECT_PREFIX___POSTGRES_DSN=postgres://user:pass@127.0.0.1:__POSTGRES_PORT__/db?sslmode=disable

# BOOTSTRAP_ENV_LINES — service env lines appended here by bootstrap.
---

RATIONALE:
- Removed all signal-cli and whatsmeow references (service-specific, not generic)
- Kept the critical '# Generated by docker_inner/up.sh' header for down.sh:26 deletion heuristic
- Added BOOTSTRAP_ENV_LINES marker for bootstrap to append service env lines
- Included placeholder pattern documentation for future service additions
- Generic example shows the __<SERVICE>_PORT__ pattern for sed substitution

DEPENDENCIES:
- Task 8 (docker_inner/ copy): ✓ Complete
- Tasks 22, 23 depend on this task
