Task 3: Delete dead op user creation block from docker/Dockerfile

REMOVED LINES (13-15):
- RUN useradd -m -s /bin/bash op \
-   && echo "op ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/op \
-   && chmod 0440 /etc/sudoers.d/op

VERIFICATION:
✓ op user block removed (grep -q 'useradd -m -s /bin/bash op' returns false)
✓ USER ubuntu still present (line 30)
✓ sudoers.d/ubuntu still present (line 27-28)

RESULT: Dead op user creation block successfully removed from Dockerfile
