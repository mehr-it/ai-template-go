Task 4: Add PROJECT_NAME fallback and explicit env_file to docker-compose.yml

CHANGES MADE:
=============

1. Updated container_name with fallback:
   OLD: container_name: ${PROJECT_NAME}-dev
   NEW: container_name: ${PROJECT_NAME:-ai-template}-dev
   
   This ensures the container defaults to "ai-template-dev" if PROJECT_NAME is not set.

2. Added explicit env_file block:
   Added before environment section:
   
   env_file:
     - path: .env
       required: false
   
   This explicitly declares the .env file with required: false, allowing the compose
   file to work even when .env doesn't exist.

VERIFICATION:
=============

✓ YAML syntax validation: PASSED
  - File parses correctly with Python yaml.safe_load()
  
✓ Container name fallback: VERIFIED
  - Line 4: container_name: ${PROJECT_NAME:-ai-template}-dev
  
✓ env_file block: VERIFIED
  - Lines 7-9: env_file section added before environment
  - Syntax: Compose Spec v3.9+ compatible

IMPACT:
=======
- Compose file now works without PROJECT_NAME environment variable
- Explicit .env declaration prevents warnings about missing .env file
- Backward compatible with existing deployments that set PROJECT_NAME
