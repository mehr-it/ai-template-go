TASK 5: Create docker/.env.example with PROJECT_NAME/UID/GID defaults
========================================================================

DELIVERABLE: docker/.env.example

FILE CONTENTS:
--------------
# Docker environment configuration template
# Copy this file to .env and customize values as needed
# The bootstrap script will rewrite PROJECT_NAME based on your setup

# Name of the dev container (bootstrap rewrites this)
PROJECT_NAME=ai-template

# Host user ID for file ownership inside container
UID=1000

# Host group ID for file ownership inside container
GID=1000

VERIFICATION:
-------------
✓ File exists and is non-empty: test -s docker/.env.example
✓ Contains PROJECT_NAME=ai-template: grep -q '^PROJECT_NAME=ai-template$'
✓ Contains UID=1000: grep -q '^UID=1000$'
✓ Contains GID=1000: grep -q '^GID=1000$'

All acceptance criteria passed.

USAGE:
------
Users should copy this file to docker/.env and customize as needed:
  cp docker/.env.example docker/.env

The docker-compose.yml references these variables:
  - PROJECT_NAME: Used for container_name (${PROJECT_NAME:-ai-template}-dev)
  - UID/GID: Used for user ownership inside container (${UID:-1000}:${GID:-1000})
